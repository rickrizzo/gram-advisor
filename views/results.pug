extends layout

block content
  div(class='container')
    h3 Top Results
    each location in data
      div(class='col-md-3 img-wrap')
        img.option(src= location.photo_url class='results img-responsive')
        p.img-desc #{location.name}
