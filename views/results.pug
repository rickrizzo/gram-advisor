extends layout

block content
  div(class='container')
    h3 Top Results
    each location in data
      div(class='col-md-3 img-wrap')
        p.img-desc #{location.name}
        div(id='myModal' class="modal fade" role="dialog")
          div(class='modal-dialog')